using System;
using System.Collections.Generic;
using System.Linq;
using DataBase.Models;

namespace GeneticAlgorithm
{
    public class Algorithm
    {
        private int maxStudentsInGroup = 5;
        /*public List<Group> RandomInitialize(List<Student> students)
        {
            var list = new List<List<Student>>();

            for (int i = 0; i < students.Count; i += maxStudentsInGroup)
            {
                list.Add(students.GetRange(i, Math.Min(maxStudentsInGroup, students.Count - i)));
            }

            foreach(var batch in list)
            {
                var 
            }

            return list;
        }*/
    }
}
